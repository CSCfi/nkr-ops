
[indexservers]
nkr-index-1 ansible_ssh_host="{{ indexserver_1_external_ip }}" ansible_host="{{ indexserver_1_internal_ip }}" ansible_connection=ssh ansible_user="{{ ssh_user }}"
nkr-index-2 ansible_ssh_host="{{ indexserver_2_external_ip }}" ansible_host="{{ indexserver_2_internal_ip }}" ansible_connection=ssh ansible_user="{{ ssh_user }}"
nkr-index-3 ansible_ssh_host="{{ indexserver_3_external_ip }}" ansible_host="{{ indexserver_3_internal_ip }}" ansible_connection=ssh ansible_user="{{ ssh_user }}"

[proxyservers]
nkr-proxy-1 ansible_host="{{ proxyserver_1_external_ip }}" ansible_connection=ssh ansible_user="{{ ssh_user }}"

[harvesterservers]
nkr-harvester-1 ansible_host="{{ harvesterserver_1_external_ip }}" ansible_connection=ssh ansible_user="{{ ssh_user }}"

[distincthosts]
nkr-host-1 ansible_host="{{ indexserver_1_external_ip }}" ansible_connection=ssh ansible_user="{{ ssh_user }}"
nkr-host-2 ansible_host="{{ indexserver_2_external_ip }}" ansible_connection=ssh ansible_user="{{ ssh_user }}"
nkr-host-3 ansible_host="{{ indexserver_3_external_ip }}" ansible_connection=ssh ansible_user="{{ ssh_user }}"
nkr-host-4 ansible_host="{{ proxyserver_1_external_ip }}" ansible_connection=ssh ansible_user="{{ ssh_user }}"
nkr-host-5 ansible_host="{{ harvesterserver_1_external_ip }}" ansible_connection=ssh ansible_user="{{ ssh_user }}"

[test:children]
indexservers
proxyservers
harvesterservers
distincthosts
